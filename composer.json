{
    "name": "swew/test",
    "description": "A test framework that is designed to fix the fatal flaw of other test frameworks.",
    "license": "MIT",
    "keywords": [
        "TDD", "testing", "fast", "lightweight", "informative interface"
    ],
    "authors": [
        {
            "name": "AVIL13",
            "email": "avil@swew.app"
        }
    ],
    "config": {
        "platform": {
            "php": ">=8.2"
        },
        "sort-packages": true,
        "optimize-autoloader": true
    },
    "bin": [
        "bin/t"
    ],
    "autoload": {
        "psr-4": {
            "Swew\\Test\\": "src/"
        },
        "files": [
            "src/Functions.php"
        ],
        "exclude-from-classmap": [
            "spec",
            "/Tests/",
            "/test/",
            "/tests/"
        ]
    },
    "archive": {
        "exclude": ["tests", "swew.json"]
    },
    "extra": {
        "branch-alias": {
            "dev-master": "1.x-dev"
        }
    },
    "prefer-stable": true,
    "repositories": [
        {
            "_": "удалить ссылку после проверки",
            "type": "path",
            "url": "../swew-cli",
            "canonical": true
        }
    ],
    "require": {
        "php": ">=8.1",
        "swew/cli": "dev-feat/cli-for-test",
        "swew/dd": "^1.3.1"
    },
    "require-dev": {
        "laravel/pint": "v1.10.0",
        "phpstan/phpstan": "^1.10.49",
        "psalm/phar": "5.10.0"
    },
    "scripts": {
        "xxx": "php ./src/TextMaster.php",
        "test": "php ./tests/Suite/RunTest.php --dir .",
        "static": "./vendor/psalm/phar/psalm.phar --no-cache",
        "stan": "vendor/bin/phpstan analyse src --level=3",
        "code-check": "./vendor/bin/pint",
        "check": [
            "@static",
            "@stan",
            "@code-check"
        ]
    }
}
